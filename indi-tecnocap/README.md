INDI driver for the TecnoSky Tecnocap cover + flat light.

# Requirements

+ INDI


# Compiling

Go to the directory where  you unpacked indi_tecnocap sources and do:

mkdir build
cd build
cmake -DCMAKE_INSTALL_PREFIX=/usr ..
make
sudo make install
